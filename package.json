{
  "name": "monitoring",
  "version": "1.0.0",
  "description": "Basic website SSL and uptime monitoring.",
  "scripts": {
    "start": "npx forever start src/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "forever": "^0.15.3",
    "superagent": "^4.1.0"
  }
}
